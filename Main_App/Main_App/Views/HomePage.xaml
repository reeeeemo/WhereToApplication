<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="Main_App.Views.HomePage"
             xmlns:vm="clr-namespace:Main_App.ViewModels"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps" 
             xmlns:local="clr-namespace:Main_App.Views"
             Title="{Binding Title}">

    <ContentPage.BindingContext>
        <vm:HomePageViewModel />
    </ContentPage.BindingContext>
    <Grid>
        <AbsoluteLayout>
            <local:CustomMap x:Name="map" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="1,1,1,1"/>
            <Frame x:Name="searchFrame" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="1,1,1,1" BackgroundColor="GhostWhite" Opacity="0.7"/>
            <Frame Padding="0" BorderColor="Transparent" BackgroundColor="Transparent" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0.01,0.01,0.2,0.05">
                <AbsoluteLayout>
                    <ImageButton x:Name="map_button" CornerRadius="5" Padding="5" Aspect="AspectFit" AbsoluteLayout.LayoutFlags="All" 
                                 AbsoluteLayout.LayoutBounds="0.07,0.5,0.45,0.85" Opacity="0.3"
                                 BackgroundColor="DimGray" Clicked="DimCurrentButton"/>
                    <ImageButton x:Name="search"  CornerRadius="5" AbsoluteLayout.LayoutFlags="All" 
                                 AbsoluteLayout.LayoutBounds="0.93,0.5,0.45,0.85"
                                 BackgroundColor="DimGray" Clicked="DimCurrentButton"/>
                </AbsoluteLayout>
            </Frame>

            <Frame AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0.05,1,1,0.8" BackgroundColor="Transparent">
                <AbsoluteLayout BackgroundColor="Transparent">
                    <Image Source="down_arrow.png" RotationX="180" BackgroundColor="Transparent" Aspect="Fill" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,1,0.17, 0.1">
                        <Image.GestureRecognizers>
                            <SwipeGestureRecognizer Direction="Down" Swiped="OpenSwipeMenu">

                            </SwipeGestureRecognizer>
                        </Image.GestureRecognizers>
                    </Image>
                    <Frame x:Name="RouteSelector" Padding="0" Margin="0" BackgroundColor="Wheat" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,2.5,0.5,0.5">
                        <ScrollView x:Name="RouteView" AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All">
                            <StackLayout Padding="5">
                                <StackLayout Orientation="Horizontal">
                                    <StackLayout.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnRouteTap">
                                            
                                        </TapGestureRecognizer>
                                    </StackLayout.GestureRecognizers>
                                    <Image BackgroundColor="Red" HeightRequest="32" WidthRequest="32" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
                                    <Label Text="501" FontSize="24" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
                                    <CheckBox Color="Red" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
                                </StackLayout>
                            </StackLayout>
                        </ScrollView>
                    </Frame>
                </AbsoluteLayout>
            </Frame>
        </AbsoluteLayout>
    </Grid>
    
</ContentPage>